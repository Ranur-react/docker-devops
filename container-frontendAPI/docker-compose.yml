version: "3.7"
services:
    web: 
        image: ranur/codeigniter:3.11-sessions
        volumes:
            - ./frontendAPI/:/app/myapp
        ports:
            - 81:8000
        networks:
            - databases
        container_name: container-frontendAPI
networks:
    databases:
        external: true
        